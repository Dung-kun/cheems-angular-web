import { ProductNameShowPipe } from './product-name-show.pipe';

describe('ProductNameShowPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductNameShowPipe();
    expect(pipe).toBeTruthy();
  });
});
