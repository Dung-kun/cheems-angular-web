import { PriceShowPipe } from './price-show.pipe';

describe('PriceShowPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceShowPipe();
    expect(pipe).toBeTruthy();
  });
});
